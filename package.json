{
  "name": "matter-sorted",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:articles": "node scripts/sync-articles.js",
    "dev": "node scripts/sync-articles.js --optional && node scripts/sync-media.js && next dev",
    "check": "node scripts/publish-check.js",
    "prebuild": "node scripts/sync-media.js",
    "build": "npm run check && next build",
    "start": "npx serve out"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "next": "^15.0.0",
    "next-mdx-remote": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "type": "module"
}
